import { TestBed } from '@angular/core/testing';
import { CalculPage } from './calcul';

describe('CalculPage', () => {

    it('should be created', () => {
        expect(new CalculPage()).toBeTruthy();
    });

    
});